const app = document.querySelector("#app");

app.innerHTML = `
  <section class="scaffold-card">
    <h1>Border Defense</h1>
    <p>
      Turret-mod frontline scaffold is in place. The full deterministic simulation, controls,
      and capture hooks land in the next commit series.
    </p>
  </section>
`;
